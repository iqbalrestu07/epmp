// @ts-nocheck
// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useTechnician, useUpdateTechnician } from "../hooks";
import { TechnicianForm } from "../components/TechnicianForm";
import type { CreateTechnicianFormData } from "../schema";

export function TechnicianEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useTechnician(id);
  const updateMutation = useUpdateTechnician();

  const handleSubmit = (formData: CreateTechnicianFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/technician/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit Technician</h1>
      <TechnicianForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
