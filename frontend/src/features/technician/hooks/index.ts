// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchTechnicians,
  fetchTechnicianById,
  createTechnician,
  updateTechnician,
  deleteTechnician,
} from "../api";
import type {
  TechnicianQueryParams,
  CreateTechnicianRequest,
  UpdateTechnicianRequest,
} from "../types";

const QUERY_KEY = "technicians";
export function useTechnicians(params?: TechnicianQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchTechnicians(params),
  });
}
export function useTechnician(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchTechnicianById(id!),
    enabled: !!id,
  });
}
export function useCreateTechnician() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateTechnicianRequest) => createTechnician(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateTechnician() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateTechnicianRequest }) =>
      updateTechnician(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteTechnician() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteTechnician(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
