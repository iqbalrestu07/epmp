// Code generated by epmp-sdk. DO NOT EDIT.

import { api, ApiError } from "@/services/api";
import type {
  Technician,
  TechnicianListResponse,
  CreateTechnicianRequest,
  UpdateTechnicianRequest,
  TechnicianQueryParams,
} from "../types";

const BASE_PATH = "/technicians";
export async function fetchTechnicians(params?: TechnicianQueryParams): Promise<TechnicianListResponse> {
  const query = new URLSearchParams();
  if (params?.page) query.set("page", String(params.page));
  if (params?.per_page) query.set("per_page", String(params.per_page));
  if (params?.search) query.set("search", params.search);
  const qs = query.toString();
  const res = await api.get<{ success: boolean; data: TechnicianListResponse }>(`${BASE_PATH}${qs ? `?${qs}` : ""}`);
  return res.data;
}
export async function fetchTechnicianById(id: string): Promise<Technician> {
  const res = await api.get<{ success: boolean; data: Technician }>(`${BASE_PATH}/${id}`);
  return res.data;
}
export async function createTechnician(data: CreateTechnicianRequest): Promise<Technician> {
  const res = await api.post<{ success: boolean; data: Technician }>(BASE_PATH, data);
  return res.data;
}
export async function updateTechnician(id: string, data: UpdateTechnicianRequest): Promise<Technician> {
  const res = await api.put<{ success: boolean; data: Technician }>(`${BASE_PATH}/${id}`, data);
  return res.data;
}
export async function deleteTechnician(id: string): Promise<void> {
  return api.delete<void>(`${BASE_PATH}/${id}`);
}

export { ApiError };
