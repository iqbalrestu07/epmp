// @ts-nocheck
// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useTenantDocument, useUpdateTenantDocument } from "../hooks";
import { TenantDocumentForm } from "../components/TenantDocumentForm";
import type { CreateTenantDocumentFormData } from "../schema";

export function TenantDocumentEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useTenantDocument(id);
  const updateMutation = useUpdateTenantDocument();

  const handleSubmit = (formData: CreateTenantDocumentFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/tenantdocument/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit TenantDocument</h1>
      <TenantDocumentForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
