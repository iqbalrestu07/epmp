// Code generated by epmp-sdk. DO NOT EDIT.

import { api, ApiError } from "@/services/api";
import type {
  TenantDocument,
  TenantDocumentListResponse,
  CreateTenantDocumentRequest,
  UpdateTenantDocumentRequest,
  TenantDocumentQueryParams,
} from "../types";

const BASE_PATH = "/api/tenant_documents";
export async function fetchTenantDocuments(params?: TenantDocumentQueryParams): Promise<TenantDocumentListResponse> {
  const query = new URLSearchParams();
  if (params?.page) query.set("page", String(params.page));
  if (params?.per_page) query.set("per_page", String(params.per_page));
  if (params?.sort) query.set("sort", params.sort);
  if (params?.order) query.set("order", params.order);
  if (params?.search) query.set("search", params.search);
  const qs = query.toString();
  return api.get<TenantDocumentListResponse>(`${BASE_PATH}${qs ? `?${qs}` : ""}`);
}
export async function fetchTenantDocumentById(id: string): Promise<TenantDocument> {
  return api.get<TenantDocument>(`${BASE_PATH}/${id}`);
}
export async function createTenantDocument(data: CreateTenantDocumentRequest): Promise<TenantDocument> {
  return api.post<TenantDocument>(BASE_PATH, data);
}
export async function updateTenantDocument(id: string, data: UpdateTenantDocumentRequest): Promise<TenantDocument> {
  return api.put<TenantDocument>(`${BASE_PATH}/${id}`, data);
}
export async function deleteTenantDocument(id: string): Promise<void> {
  return api.delete<void>(`${BASE_PATH}/${id}`);
}

export { ApiError };
