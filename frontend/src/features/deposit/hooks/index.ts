// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchDeposits,
  fetchDepositById,
  createDeposit,
  updateDeposit,
  deleteDeposit,
} from "../api";
import type {
  DepositQueryParams,
  CreateDepositRequest,
  UpdateDepositRequest,
} from "../types";

const QUERY_KEY = "deposits";
export function useDeposits(params?: DepositQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchDeposits(params),
  });
}
export function useDeposit(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchDepositById(id!),
    enabled: !!id,
  });
}
export function useCreateDeposit() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateDepositRequest) => createDeposit(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateDeposit() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateDepositRequest }) =>
      updateDeposit(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteDeposit() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteDeposit(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
