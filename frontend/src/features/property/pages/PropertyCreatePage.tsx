// @ts-nocheck
// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate } from "react-router-dom";
import { useCreateProperty } from "../hooks";
import { PropertyForm } from "../components/PropertyForm";
import type { CreatePropertyFormData } from "../schema";

export function PropertyCreatePage() {
  const navigate = useNavigate();
  const createMutation = useCreateProperty();

  const handleSubmit = (data: CreatePropertyFormData) => {
    createMutation.mutate(data, {
      onSuccess: () => navigate("/property"),
    });
  };

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">New Property</h1>
      <PropertyForm
        onSubmit={handleSubmit}
        isSubmitting={createMutation.isPending}
      />
    </div>
  );
}
