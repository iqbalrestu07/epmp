// @ts-nocheck
// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useWorkOrder, useUpdateWorkOrder } from "../hooks";
import { WorkOrderForm } from "../components/WorkOrderForm";
import type { CreateWorkOrderFormData } from "../schema";

export function WorkOrderEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useWorkOrder(id);
  const updateMutation = useUpdateWorkOrder();

  const handleSubmit = (formData: CreateWorkOrderFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/workorder/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit WorkOrder</h1>
      <WorkOrderForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
