// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useAdjustment, useUpdateAdjustment } from "../hooks";
import { AdjustmentForm } from "../components/AdjustmentForm";
import type { CreateAdjustmentFormData } from "../schema";

export function AdjustmentEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useAdjustment(id);
  const updateMutation = useUpdateAdjustment();

  const handleSubmit = (formData: CreateAdjustmentFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/adjustment/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit Adjustment</h1>
      <AdjustmentForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
