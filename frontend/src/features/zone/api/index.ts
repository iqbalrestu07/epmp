// Code generated by epmp-sdk. DO NOT EDIT.

import { api, ApiError } from "@/services/api";
import type {
  Zone,
  ZoneListResponse,
  CreateZoneRequest,
  UpdateZoneRequest,
  ZoneQueryParams,
} from "../types";

const BASE_PATH = "/zones";
export async function fetchZones(params?: ZoneQueryParams): Promise<ZoneListResponse> {
  const query = new URLSearchParams();
  if (params?.page) query.set("page", String(params.page));
  if (params?.per_page) query.set("per_page", String(params.per_page));
  if (params?.search) query.set("search", params.search);
  const qs = query.toString();
  const res = await api.get<{ success: boolean; data: ZoneListResponse }>(`${BASE_PATH}${qs ? `?${qs}` : ""}`);
  return res.data;
}
export async function fetchZoneById(id: string): Promise<Zone> {
  const res = await api.get<{ success: boolean; data: Zone }>(`${BASE_PATH}/${id}`);
  return res.data;
}
export async function createZone(data: CreateZoneRequest): Promise<Zone> {
  const res = await api.post<{ success: boolean; data: Zone }>(BASE_PATH, data);
  return res.data;
}
export async function updateZone(id: string, data: UpdateZoneRequest): Promise<Zone> {
  const res = await api.put<{ success: boolean; data: Zone }>(`${BASE_PATH}/${id}`, data);
  return res.data;
}
export async function deleteZone(id: string): Promise<void> {
  return api.delete<void>(`${BASE_PATH}/${id}`);
}

export { ApiError };
