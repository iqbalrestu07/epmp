// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate } from "react-router-dom";
import { useCreateZone } from "../hooks";
import { ZoneForm } from "../components/ZoneForm";
import type { CreateZoneFormData } from "../schema";

export function ZoneCreatePage() {
  const navigate = useNavigate();
  const createMutation = useCreateZone();

  const handleSubmit = (data: CreateZoneFormData) => {
    createMutation.mutate(data, {
      onSuccess: () => navigate("/zone"),
    });
  };

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">New Zone</h1>
      <ZoneForm
        onSubmit={handleSubmit}
        isSubmitting={createMutation.isPending}
      />
    </div>
  );
}
