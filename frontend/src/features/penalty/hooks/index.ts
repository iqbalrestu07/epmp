// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchPenaltys,
  fetchPenaltyById,
  createPenalty,
  updatePenalty,
  deletePenalty,
} from "../api";
import type {
  PenaltyQueryParams,
  CreatePenaltyRequest,
  UpdatePenaltyRequest,
} from "../types";

const QUERY_KEY = "penaltys";
export function usePenaltys(params?: PenaltyQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchPenaltys(params),
  });
}
export function usePenalty(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchPenaltyById(id!),
    enabled: !!id,
  });
}
export function useCreatePenalty() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreatePenaltyRequest) => createPenalty(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdatePenalty() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdatePenaltyRequest }) =>
      updatePenalty(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeletePenalty() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deletePenalty(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
