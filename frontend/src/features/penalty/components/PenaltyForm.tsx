// Code generated by epmp-sdk. DO NOT EDIT.

import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import {
  createPenaltySchema,
  type CreatePenaltyFormData,
} from "../schema";

interface PenaltyFormProps {
  onSubmit: (data: CreatePenaltyFormData) => void;
  defaultValues?: Partial<CreatePenaltyFormData>;
  isSubmitting?: boolean;
}

export function PenaltyForm({
  onSubmit,
  defaultValues,
  isSubmitting,
}: PenaltyFormProps) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<CreatePenaltyFormData>({
    resolver: zodResolver(createPenaltySchema),
    defaultValues,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="organization_id">OrganizationId</Label>
        <Input
          id="organization_id"
          {...register("organization_id")}
        />
        {errors.organization_id && (
          <p className="text-sm text-red-500">{errors.organization_id.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="invoice_id">InvoiceId</Label>
        <Input
          id="invoice_id"
          {...register("invoice_id")}
        />
        {errors.invoice_id && (
          <p className="text-sm text-red-500">{errors.invoice_id.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="amount">Amount</Label>
        <Input
          id="amount"
          {...register("amount")}
        />
        {errors.amount && (
          <p className="text-sm text-red-500">{errors.amount.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="status">Status</Label>
        <Input
          id="status"
          {...register("status")}
        />
        {errors.status && (
          <p className="text-sm text-red-500">{errors.status.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="penalty_date">PenaltyDate</Label>
        <Input
          id="penalty_date"
          {...register("penalty_date")}
        />
        {errors.penalty_date && (
          <p className="text-sm text-red-500">{errors.penalty_date.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="description">Description</Label>
        <Input
          id="description"
          {...register("description")}
        />
        {errors.description && (
          <p className="text-sm text-red-500">{errors.description.message}</p>
        )}
      </div>

      <Button type="submit" disabled={isSubmitting}>
        {isSubmitting ? "Saving..." : "Save"}
      </Button>
    </form>
  );
}
