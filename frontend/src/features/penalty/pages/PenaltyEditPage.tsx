// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { usePenalty, useUpdatePenalty } from "../hooks";
import { PenaltyForm } from "../components/PenaltyForm";
import type { CreatePenaltyFormData } from "../schema";

export function PenaltyEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = usePenalty(id);
  const updateMutation = useUpdatePenalty();

  const handleSubmit = (formData: CreatePenaltyFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/penalty/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit Penalty</h1>
      <PenaltyForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
