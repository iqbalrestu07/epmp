// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useOrganization, useUpdateOrganization } from "../hooks";
import { OrganizationForm } from "../components/OrganizationForm";
import type { CreateOrganizationFormData } from "../schema";

export function OrganizationEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useOrganization(id);
  const updateMutation = useUpdateOrganization();

  const handleSubmit = (formData: CreateOrganizationFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/organization/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit Organization</h1>
      <OrganizationForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
