// Code generated by epmp-sdk. DO NOT EDIT.

import { useParams, useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { useAsset, useDeleteAsset } from "../hooks";

export function AssetDetailPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useAsset(id);
  const deleteMutation = useDeleteAsset();

  const handleDelete = () => {
    if (!confirm("Are you sure you want to delete this asset?")) return;
    deleteMutation.mutate(id!, {
      onSuccess: () => navigate("/asset"),
    });
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">Asset Detail</h1>
        <div className="flex gap-2">
          <Button variant="outline" onClick={() => navigate("/asset")}>
            Back
          </Button>
          <Button variant="outline" onClick={() => navigate(`/asset/${id}/edit`)}>
            Edit
          </Button>
          <Button variant="destructive" onClick={handleDelete}>
            Delete
          </Button>
        </div>
      </div>

      <dl className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">OrganizationId</dt>
          <dd className="text-sm">{String(data.organization_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Id</dt>
          <dd className="text-sm">{String(data.id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">PropertyId</dt>
          <dd className="text-sm">{String(data.property_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Name</dt>
          <dd className="text-sm">{String(data.name ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Category</dt>
          <dd className="text-sm">{String(data.category ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Status</dt>
          <dd className="text-sm">{String(data.status ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">PurchasePrice</dt>
          <dd className="text-sm">{String(data.purchase_price ?? "—")}</dd>
        </div>
      </dl>
    </div>
  );
}
