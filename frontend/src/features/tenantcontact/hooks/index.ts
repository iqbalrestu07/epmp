// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchTenantContacts,
  fetchTenantContactById,
  createTenantContact,
  updateTenantContact,
  deleteTenantContact,
} from "../api";
import type {
  TenantContactQueryParams,
  CreateTenantContactRequest,
  UpdateTenantContactRequest,
} from "../types";

const QUERY_KEY = "tenantContacts";
export function useTenantContacts(params?: TenantContactQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchTenantContacts(params),
  });
}
export function useTenantContact(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchTenantContactById(id!),
    enabled: !!id,
  });
}
export function useCreateTenantContact() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateTenantContactRequest) => createTenantContact(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateTenantContact() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateTenantContactRequest }) =>
      updateTenantContact(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteTenantContact() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteTenantContact(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
