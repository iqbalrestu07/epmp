// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate } from "react-router-dom";
import { useCreateTenantContact } from "../hooks";
import { TenantContactForm } from "../components/TenantContactForm";
import type { CreateTenantContactFormData } from "../schema";

export function TenantContactCreatePage() {
  const navigate = useNavigate();
  const createMutation = useCreateTenantContact();

  const handleSubmit = (data: CreateTenantContactFormData) => {
    createMutation.mutate(data, {
      onSuccess: () => navigate("/tenantcontact"),
    });
  };

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">New TenantContact</h1>
      <TenantContactForm
        onSubmit={handleSubmit}
        isSubmitting={createMutation.isPending}
      />
    </div>
  );
}
