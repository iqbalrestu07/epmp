// Code generated by epmp-sdk. DO NOT EDIT.

import { useParams, useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { useTenantContact, useDeleteTenantContact } from "../hooks";

export function TenantContactDetailPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useTenantContact(id);
  const deleteMutation = useDeleteTenantContact();

  const handleDelete = () => {
    if (!confirm("Are you sure you want to delete this tenantContact?")) return;
    deleteMutation.mutate(id!, {
      onSuccess: () => navigate("/tenantcontact"),
    });
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">TenantContact Detail</h1>
        <div className="flex gap-2">
          <Button variant="outline" onClick={() => navigate("/tenantcontact")}>
            Back
          </Button>
          <Button variant="outline" onClick={() => navigate(`/tenantcontact/${id}/edit`)}>
            Edit
          </Button>
          <Button variant="destructive" onClick={handleDelete}>
            Delete
          </Button>
        </div>
      </div>

      <dl className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">OrganizationId</dt>
          <dd className="text-sm">{String(data.organization_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Id</dt>
          <dd className="text-sm">{String(data.id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">TenantId</dt>
          <dd className="text-sm">{String(data.tenant_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">ContactType</dt>
          <dd className="text-sm">{String(data.contact_type ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">ContactValue</dt>
          <dd className="text-sm">{String(data.contact_value ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">IsPrimary</dt>
          <dd className="text-sm">{String(data.is_primary ?? "—")}</dd>
        </div>
      </dl>
    </div>
  );
}
