// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchFacilitys,
  fetchFacilityById,
  createFacility,
  updateFacility,
  deleteFacility,
} from "../api";
import type {
  FacilityQueryParams,
  CreateFacilityRequest,
  UpdateFacilityRequest,
} from "../types";

const QUERY_KEY = "facilitys";
export function useFacilitys(params?: FacilityQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchFacilitys(params),
  });
}
export function useFacility(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchFacilityById(id!),
    enabled: !!id,
  });
}
export function useCreateFacility() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateFacilityRequest) => createFacility(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateFacility() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateFacilityRequest }) =>
      updateFacility(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteFacility() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteFacility(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
