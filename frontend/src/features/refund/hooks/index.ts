// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchRefunds,
  fetchRefundById,
  createRefund,
  updateRefund,
  deleteRefund,
} from "../api";
import type {
  RefundQueryParams,
  CreateRefundRequest,
  UpdateRefundRequest,
} from "../types";

const QUERY_KEY = "refunds";
export function useRefunds(params?: RefundQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchRefunds(params),
  });
}
export function useRefund(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchRefundById(id!),
    enabled: !!id,
  });
}
export function useCreateRefund() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateRefundRequest) => createRefund(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateRefund() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateRefundRequest }) =>
      updateRefund(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteRefund() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteRefund(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
