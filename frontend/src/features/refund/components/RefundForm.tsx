// Code generated by epmp-sdk. DO NOT EDIT.

import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import {
  createRefundSchema,
  type CreateRefundFormData,
} from "../schema";

interface RefundFormProps {
  onSubmit: (data: CreateRefundFormData) => void;
  defaultValues?: Partial<CreateRefundFormData>;
  isSubmitting?: boolean;
}

export function RefundForm({
  onSubmit,
  defaultValues,
  isSubmitting,
}: RefundFormProps) {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<CreateRefundFormData>({
    resolver: zodResolver(createRefundSchema),
    defaultValues,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="organization_id">OrganizationId</Label>
        <Input
          id="organization_id"
          {...register("organization_id")}
        />
        {errors.organization_id && (
          <p className="text-sm text-red-500">{errors.organization_id.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="payment_id">PaymentId</Label>
        <Input
          id="payment_id"
          {...register("payment_id")}
        />
        {errors.payment_id && (
          <p className="text-sm text-red-500">{errors.payment_id.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="tenant_id">TenantId</Label>
        <Input
          id="tenant_id"
          {...register("tenant_id")}
        />
        {errors.tenant_id && (
          <p className="text-sm text-red-500">{errors.tenant_id.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="amount">Amount</Label>
        <Input
          id="amount"
          {...register("amount")}
        />
        {errors.amount && (
          <p className="text-sm text-red-500">{errors.amount.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="status">Status</Label>
        <Input
          id="status"
          {...register("status")}
        />
        {errors.status && (
          <p className="text-sm text-red-500">{errors.status.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="refund_date">RefundDate</Label>
        <Input
          id="refund_date"
          {...register("refund_date")}
        />
        {errors.refund_date && (
          <p className="text-sm text-red-500">{errors.refund_date.message}</p>
        )}
      </div>
      <div className="space-y-2">
        <Label htmlFor="reason">Reason</Label>
        <Input
          id="reason"
          {...register("reason")}
        />
        {errors.reason && (
          <p className="text-sm text-red-500">{errors.reason.message}</p>
        )}
      </div>

      <Button type="submit" disabled={isSubmitting}>
        {isSubmitting ? "Saving..." : "Save"}
      </Button>
    </form>
  );
}
