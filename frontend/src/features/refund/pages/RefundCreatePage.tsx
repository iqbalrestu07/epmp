// @ts-nocheck
// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate } from "react-router-dom";
import { useCreateRefund } from "../hooks";
import { RefundForm } from "../components/RefundForm";
import type { CreateRefundFormData } from "../schema";

export function RefundCreatePage() {
  const navigate = useNavigate();
  const createMutation = useCreateRefund();

  const handleSubmit = (data: CreateRefundFormData) => {
    createMutation.mutate(data, {
      onSuccess: () => navigate("/refund"),
    });
  };

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">New Refund</h1>
      <RefundForm
        onSubmit={handleSubmit}
        isSubmitting={createMutation.isPending}
      />
    </div>
  );
}
