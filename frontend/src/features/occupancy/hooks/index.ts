// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchOccupancys,
  fetchOccupancyById,
  createOccupancy,
  updateOccupancy,
  deleteOccupancy,
} from "../api";
import type {
  OccupancyQueryParams,
  CreateOccupancyRequest,
  UpdateOccupancyRequest,
} from "../types";

const QUERY_KEY = "occupancys";
export function useOccupancys(params?: OccupancyQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchOccupancys(params),
  });
}
export function useOccupancy(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchOccupancyById(id!),
    enabled: !!id,
  });
}
export function useCreateOccupancy() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateOccupancyRequest) => createOccupancy(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateOccupancy() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateOccupancyRequest }) =>
      updateOccupancy(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteOccupancy() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteOccupancy(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
