// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate } from "react-router-dom";
import { useCreateOccupancy } from "../hooks";
import { OccupancyForm } from "../components/OccupancyForm";
import type { CreateOccupancyFormData } from "../schema";

export function OccupancyCreatePage() {
  const navigate = useNavigate();
  const createMutation = useCreateOccupancy();

  const handleSubmit = (data: CreateOccupancyFormData) => {
    createMutation.mutate(data, {
      onSuccess: () => navigate("/occupancy"),
    });
  };

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">New Occupancy</h1>
      <OccupancyForm
        onSubmit={handleSubmit}
        isSubmitting={createMutation.isPending}
      />
    </div>
  );
}
