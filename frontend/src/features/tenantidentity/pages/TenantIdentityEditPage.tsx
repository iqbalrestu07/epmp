// @ts-nocheck
// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useTenantIdentity, useUpdateTenantIdentity } from "../hooks";
import { TenantIdentityForm } from "../components/TenantIdentityForm";
import type { CreateTenantIdentityFormData } from "../schema";

export function TenantIdentityEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useTenantIdentity(id);
  const updateMutation = useUpdateTenantIdentity();

  const handleSubmit = (formData: CreateTenantIdentityFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/tenantidentity/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit TenantIdentity</h1>
      <TenantIdentityForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
