// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchRoomTypes,
  fetchRoomTypeById,
  createRoomType,
  updateRoomType,
  deleteRoomType,
} from "../api";
import type {
  RoomTypeQueryParams,
  CreateRoomTypeRequest,
  UpdateRoomTypeRequest,
} from "../types";

const QUERY_KEY = "roomTypes";
export function useRoomTypes(params?: RoomTypeQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchRoomTypes(params),
  });
}
export function useRoomType(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchRoomTypeById(id!),
    enabled: !!id,
  });
}
export function useCreateRoomType() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateRoomTypeRequest) => createRoomType(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateRoomType() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateRoomTypeRequest }) =>
      updateRoomType(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteRoomType() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteRoomType(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
