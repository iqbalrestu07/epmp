// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { useRoomType, useUpdateRoomType } from "../hooks";
import { RoomTypeForm } from "../components/RoomTypeForm";
import type { CreateRoomTypeFormData } from "../schema";

export function RoomTypeEditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useRoomType(id);
  const updateMutation = useUpdateRoomType();

  const handleSubmit = (formData: CreateRoomTypeFormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/roomtype/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit RoomType</h1>
      <RoomTypeForm
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
