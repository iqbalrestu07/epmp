// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetchBeds,
  fetchBedById,
  createBed,
  updateBed,
  deleteBed,
} from "../api";
import type {
  BedQueryParams,
  CreateBedRequest,
  UpdateBedRequest,
} from "../types";

const QUERY_KEY = "beds";
export function useBeds(params?: BedQueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetchBeds(params),
  });
}
export function useBed(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetchBedById(id!),
    enabled: !!id,
  });
}
export function useCreateBed() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateBedRequest) => createBed(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useUpdateBed() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: UpdateBedRequest }) =>
      updateBed(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
export function useDeleteBed() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => deleteBed(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
