// Code generated by epmp-sdk. DO NOT EDIT.

import { useParams, useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { useCharge, useDeleteCharge } from "../hooks";

export function ChargeDetailPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = useCharge(id);
  const deleteMutation = useDeleteCharge();

  const handleDelete = () => {
    if (!confirm("Are you sure you want to delete this charge?")) return;
    deleteMutation.mutate(id!, {
      onSuccess: () => navigate("/charge"),
    });
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">Charge Detail</h1>
        <div className="flex gap-2">
          <Button variant="outline" onClick={() => navigate("/charge")}>
            Back
          </Button>
          <Button variant="outline" onClick={() => navigate(`/charge/${id}/edit`)}>
            Edit
          </Button>
          <Button variant="destructive" onClick={handleDelete}>
            Delete
          </Button>
        </div>
      </div>

      <dl className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">OrganizationId</dt>
          <dd className="text-sm">{String(data.organization_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Id</dt>
          <dd className="text-sm">{String(data.id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">ContractId</dt>
          <dd className="text-sm">{String(data.contract_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">InvoiceId</dt>
          <dd className="text-sm">{String(data.invoice_id ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">ChargeType</dt>
          <dd className="text-sm">{String(data.charge_type ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Amount</dt>
          <dd className="text-sm">{String(data.amount ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Status</dt>
          <dd className="text-sm">{String(data.status ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">ChargeDate</dt>
          <dd className="text-sm">{String(data.charge_date ?? "—")}</dd>
        </div>
        <div className="space-y-1">
          <dt className="text-sm font-medium text-gray-500">Notes</dt>
          <dd className="text-sm">{String(data.notes ?? "—")}</dd>
        </div>
      </dl>
    </div>
  );
}
