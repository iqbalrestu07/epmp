// Code generated by epmp-sdk. DO NOT EDIT.

import {
  useQuery,
  useMutation,
  useQueryClient,
} from "@tanstack/react-query";
import {
  fetch{{.Name}}s,
  fetch{{.Name}}ById,
  create{{.Name}},
  update{{.Name}},
  delete{{.Name}},
} from "../api";
import type {
  {{.Name}}QueryParams,
  Create{{.Name}}Request,
  Update{{.Name}}Request,
} from "../types";

const QUERY_KEY = "{{.NameLower}}s";

{{- if .HasList}}
export function use{{.Name}}s(params?: {{.Name}}QueryParams) {
  return useQuery({
    queryKey: [QUERY_KEY, params],
    queryFn: () => fetch{{.Name}}s(params),
  });
}
{{- end}}

{{- if .HasRead}}
export function use{{.Name}}(id: string | undefined) {
  return useQuery({
    queryKey: [QUERY_KEY, id],
    queryFn: () => fetch{{.Name}}ById(id!),
    enabled: !!id,
  });
}
{{- end}}

{{- if .HasCreate}}
export function useCreate{{.Name}}() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: Create{{.Name}}Request) => create{{.Name}}(data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
{{- end}}

{{- if .HasUpdate}}
export function useUpdate{{.Name}}() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, data }: { id: string; data: Update{{.Name}}Request }) =>
      update{{.Name}}(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
{{- end}}

{{- if .HasDelete}}
export function useDelete{{.Name}}() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => delete{{.Name}}(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: [QUERY_KEY] });
    },
  });
}
{{- end}}
