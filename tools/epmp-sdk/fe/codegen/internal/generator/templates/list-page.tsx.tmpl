// Code generated by epmp-sdk. DO NOT EDIT.

import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { {{.Name}}Table } from "../components/{{.Name}}Table";
import { use{{.Name}}s, useDelete{{.Name}} } from "../hooks";
import type { {{.Name}} } from "../types";

export function {{.Name}}ListPage() {
  const navigate = useNavigate();
  const [page, setPage] = useState(1);
  const [search, setSearch] = useState("");
{{- if .HasSearch}}
  const [searchInput, setSearchInput] = useState("");
{{- end}}

  const { data, isLoading } = use{{.Name}}s({
    page,
    per_page: 20,
{{- if .HasSearch}}
    search: search || undefined,
{{- end}}
  });

  const deleteMutation = useDelete{{.Name}}();

  const handleRowClick = (row: {{.Name}}) => {
    navigate(`/{{.Package}}/${row.id}`);
  };

{{- if .HasSearch}}
  const handleSearch = (e: React.FormEvent) => {
    e.preventDefault();
    setSearch(searchInput);
    setPage(1);
  };
{{- end}}

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">{{.Name}}s</h1>
        <Button onClick={() => navigate("/{{.Package}}/new")}>
          New {{.Name}}
        </Button>
      </div>

{{- if .HasSearch}}
      <form onSubmit={handleSearch} className="flex gap-2">
        <Input
          placeholder="Search..."
          value={searchInput}
          onChange={(e) => setSearchInput(e.target.value)}
          className="max-w-sm"
        />
        <Button type="submit" variant="outline">Search</Button>
      </form>
{{- end}}

      {isLoading ? (
        <div className="text-center py-8">Loading...</div>
      ) : (
        <>
          <{{.Name}}Table data={data?.data ?? []} onRowClick={handleRowClick} />

          {data && data.total_pages > 1 && (
            <div className="flex items-center justify-between">
              <p className="text-sm text-gray-500">
                Page {data.page} of {data.total_pages} ({data.total} total)
              </p>
              <div className="flex gap-2">
                <Button
                  variant="outline"
                  size="sm"
                  disabled={page <= 1}
                  onClick={() => setPage(page - 1)}
                >
                  Previous
                </Button>
                <Button
                  variant="outline"
                  size="sm"
                  disabled={page >= data.total_pages}
                  onClick={() => setPage(page + 1)}
                >
                  Next
                </Button>
              </div>
            </div>
          )}
        </>
      )}
    </div>
  );
}
