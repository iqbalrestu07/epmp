// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate, useParams } from "react-router-dom";
import { use{{.Name}}, useUpdate{{.Name}} } from "../hooks";
import { {{.Name}}Form } from "../components/{{.Name}}Form";
import type { Create{{.Name}}FormData } from "../schema";

export function {{.Name}}EditPage() {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const { data, isLoading } = use{{.Name}}(id);
  const updateMutation = useUpdate{{.Name}}();

  const handleSubmit = (formData: Create{{.Name}}FormData) => {
    updateMutation.mutate(
      { id: id!, data: formData },
      { onSuccess: () => navigate(`/{{.Package}}/${id}`) }
    );
  };

  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Edit {{.Name}}</h1>
      <{{.Name}}Form
        onSubmit={handleSubmit}
        defaultValues={data}
        isSubmitting={updateMutation.isPending}
      />
    </div>
  );
}
