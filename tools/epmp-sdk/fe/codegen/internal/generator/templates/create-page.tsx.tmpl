// Code generated by epmp-sdk. DO NOT EDIT.

import { useNavigate } from "react-router-dom";
import { useCreate{{.Name}} } from "../hooks";
import { {{.Name}}Form } from "../components/{{.Name}}Form";
import type { Create{{.Name}}FormData } from "../schema";

export function {{.Name}}CreatePage() {
  const navigate = useNavigate();
  const createMutation = useCreate{{.Name}}();

  const handleSubmit = (data: Create{{.Name}}FormData) => {
    createMutation.mutate(data, {
      onSuccess: () => navigate("/{{.Package}}"),
    });
  };

  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">New {{.Name}}</h1>
      <{{.Name}}Form
        onSubmit={handleSubmit}
        isSubmitting={createMutation.isPending}
      />
    </div>
  );
}
