// Code generated by epmp-sdk. DO NOT EDIT.

package dto

import "time"

// Create{{.Name}}Request is the DTO for creating a {{.Name}}.
type Create{{.Name}}Request struct {
{{- range .Fields}}
{{- if not .PrimaryKey}}
	{{.Name}} {{.GoType}} `json:"{{.JSONName}}"`
{{- end}}
{{- end}}
}

// Update{{.Name}}Request is the DTO for updating a {{.Name}}.
type Update{{.Name}}Request struct {
{{- range .Fields}}
{{- if not .PrimaryKey}}
	{{.Name}} {{.GoType}} `json:"{{.JSONName}}"`
{{- end}}
{{- end}}
}

// {{.Name}}Response is the DTO for returning a {{.Name}}.
type {{.Name}}Response struct {
{{- range .Fields}}
	{{.Name}} {{.GoType}} `json:"{{.JSONName}}"`
{{- end}}
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}

// {{.Name}}ListResponse is the DTO for a paginated list of {{.Name}}.
type {{.Name}}ListResponse struct {
	Data       []{{.Name}}Response `json:"data"`
	Total      int64               `json:"total"`
	Page       int                 `json:"page"`
	PerPage    int                 `json:"per_page"`
	TotalPages int                 `json:"total_pages"`
}
