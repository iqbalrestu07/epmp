// Code generated by epmp-sdk. DO NOT EDIT.

package repository

import (
	"context"
	"fmt"

	"github.com/epmp/backend/internal/modules/floor/entity"

	"github.com/jackc/pgx/v5/pgxpool"
)

// FloorRepositoryImpl implements FloorRepository using PostgreSQL.
type FloorRepositoryImpl struct {
	db *pgxpool.Pool
}

// NewFloorRepositoryImpl creates a new FloorRepositoryImpl.
func NewFloorRepositoryImpl(db *pgxpool.Pool) *FloorRepositoryImpl {
	return &FloorRepositoryImpl{db: db}
}

// Ensure FloorRepositoryImpl implements domain repository interface.
var _ FloorRepository = (*FloorRepositoryImpl)(nil)

func (r *FloorRepositoryImpl) Save(ctx context.Context, e *entity.Floor) error {
	if e.Id == "" {
		// INSERT
		err := r.db.QueryRow(ctx, `
			INSERT INTO floors (organization_id, building_id, name, floor_number, is_active)
			VALUES ($1, $2, $3, $4, $5)
			RETURNING id`,
			e.OrganizationId, e.BuildingId, e.Name, e.FloorNumber, e.IsActive,
		).Scan(&e.Id)
		return err
	}
	// UPDATE
	// Note: id is dynamically added as the last argument in Exec for UPDATE
	_, err := r.db.Exec(ctx, `
		UPDATE floors
		SET    organization_id=$1, building_id=$2, name=$3, floor_number=$4, is_active=$5
		WHERE  id=$6 AND deleted_at IS NULL`,
		e.OrganizationId, e.BuildingId, e.Name, e.FloorNumber, e.IsActive, e.Id,
	)
	return err
}

func (r *FloorRepositoryImpl) FindByID(ctx context.Context, id string) (*entity.Floor, error) {
	e := &entity.Floor{}
	err := r.db.QueryRow(ctx, `
		SELECT id, organization_id, building_id, name, floor_number, is_active, deleted_at
		FROM   floors
		WHERE  id = $1 AND deleted_at IS NULL`,
		id,
	).Scan(&e.Id, &e.OrganizationId, &e.BuildingId, &e.Name, &e.FloorNumber, &e.IsActive, &e.DeletedAt)
	
	if err != nil {
		return nil, fmt.Errorf("floor repository: find by id: %w", err)
	}
	return e, nil
}

func (r *FloorRepositoryImpl) FindAll(ctx context.Context, limit, offset int) ([]*entity.Floor, error) {
	rows, err := r.db.Query(ctx, `
		SELECT id, organization_id, building_id, name, floor_number, is_active, deleted_at
		FROM   floors
		WHERE  deleted_at IS NULL
		ORDER BY created_at DESC
		LIMIT $1 OFFSET $2`,
		limit, offset,
	)
	if err != nil {
		return nil, fmt.Errorf("floor repository: find all: %w", err)
	}
	defer rows.Close()

	var list []*entity.Floor
	for rows.Next() {
		e := &entity.Floor{}
		if err := rows.Scan(&e.Id, &e.OrganizationId, &e.BuildingId, &e.Name, &e.FloorNumber, &e.IsActive, &e.DeletedAt); err != nil {
			return nil, err
		}
		list = append(list, e)
	}
	return list, rows.Err()
}

func (r *FloorRepositoryImpl) Delete(ctx context.Context, id string) error {
	_, err := r.db.Exec(ctx, `
		UPDATE floors SET deleted_at = now() WHERE id = $1 AND deleted_at IS NULL`, id)
	return err
}
