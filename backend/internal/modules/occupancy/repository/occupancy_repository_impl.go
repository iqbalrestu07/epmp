// Code generated by epmp-sdk. DO NOT EDIT.

package repository

import (
	"context"
	"fmt"

	"github.com/epmp/backend/internal/modules/occupancy/entity"

	"github.com/jackc/pgx/v5/pgxpool"
)

// OccupancyRepositoryImpl implements OccupancyRepository using PostgreSQL.
type OccupancyRepositoryImpl struct {
	db *pgxpool.Pool
}

// NewOccupancyRepositoryImpl creates a new OccupancyRepositoryImpl.
func NewOccupancyRepositoryImpl(db *pgxpool.Pool) *OccupancyRepositoryImpl {
	return &OccupancyRepositoryImpl{db: db}
}

// Ensure OccupancyRepositoryImpl implements domain repository interface.
var _ OccupancyRepository = (*OccupancyRepositoryImpl)(nil)

func (r *OccupancyRepositoryImpl) Save(ctx context.Context, e *entity.Occupancy) error {
	if e.Id == "" {
		// INSERT
		err := r.db.QueryRow(ctx, `
			INSERT INTO occupancies (organization_id, contract_id, room_id, tenant_id, status, check_in_time, check_out_time, notes)
			VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
			RETURNING id`,
			e.OrganizationId, e.ContractId, e.RoomId, e.TenantId, e.Status, e.CheckInTime, e.CheckOutTime, e.Notes,
		).Scan(&e.Id)
		return err
	}
	// UPDATE
	// Note: id is dynamically added as the last argument in Exec for UPDATE
	_, err := r.db.Exec(ctx, `
		UPDATE occupancies
		SET    organization_id=$1, contract_id=$2, room_id=$3, tenant_id=$4, status=$5, check_in_time=$6, check_out_time=$7, notes=$8
		WHERE  id=$9 AND deleted_at IS NULL`,
		e.OrganizationId, e.ContractId, e.RoomId, e.TenantId, e.Status, e.CheckInTime, e.CheckOutTime, e.Notes, e.Id,
	)
	return err
}

func (r *OccupancyRepositoryImpl) FindByID(ctx context.Context, id string) (*entity.Occupancy, error) {
	e := &entity.Occupancy{}
	err := r.db.QueryRow(ctx, `
		SELECT organization_id, id, contract_id, room_id, tenant_id, status, check_in_time, check_out_time, notes, deleted_at
		FROM   occupancies
		WHERE  id = $1 AND deleted_at IS NULL`,
		id,
	).Scan(&e.OrganizationId, &e.Id, &e.ContractId, &e.RoomId, &e.TenantId, &e.Status, &e.CheckInTime, &e.CheckOutTime, &e.Notes, &e.DeletedAt)
	
	if err != nil {
		return nil, fmt.Errorf("occupancy repository: find by id: %w", err)
	}
	return e, nil
}

func (r *OccupancyRepositoryImpl) FindAll(ctx context.Context, limit, offset int) ([]*entity.Occupancy, error) {
	rows, err := r.db.Query(ctx, `
		SELECT organization_id, id, contract_id, room_id, tenant_id, status, check_in_time, check_out_time, notes, deleted_at
		FROM   occupancies
		WHERE  deleted_at IS NULL
		ORDER BY created_at DESC
		LIMIT $1 OFFSET $2`,
		limit, offset,
	)
	if err != nil {
		return nil, fmt.Errorf("occupancy repository: find all: %w", err)
	}
	defer rows.Close()

	var list []*entity.Occupancy
	for rows.Next() {
		e := &entity.Occupancy{}
		if err := rows.Scan(&e.OrganizationId, &e.Id, &e.ContractId, &e.RoomId, &e.TenantId, &e.Status, &e.CheckInTime, &e.CheckOutTime, &e.Notes, &e.DeletedAt); err != nil {
			return nil, err
		}
		list = append(list, e)
	}
	return list, rows.Err()
}

func (r *OccupancyRepositoryImpl) Delete(ctx context.Context, id string) error {
	_, err := r.db.Exec(ctx, `
		UPDATE occupancies SET deleted_at = now() WHERE id = $1 AND deleted_at IS NULL`, id)
	return err
}
