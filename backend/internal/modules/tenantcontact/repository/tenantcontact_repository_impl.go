// Code generated by epmp-sdk. DO NOT EDIT.

package repository

import (
	"context"
	"fmt"

	"github.com/epmp/backend/internal/modules/tenantcontact/entity"

	"github.com/jackc/pgx/v5/pgxpool"
)

// TenantContactRepositoryImpl implements TenantContactRepository using PostgreSQL.
type TenantContactRepositoryImpl struct {
	db *pgxpool.Pool
}

// NewTenantContactRepositoryImpl creates a new TenantContactRepositoryImpl.
func NewTenantContactRepositoryImpl(db *pgxpool.Pool) *TenantContactRepositoryImpl {
	return &TenantContactRepositoryImpl{db: db}
}

// Ensure TenantContactRepositoryImpl implements domain repository interface.
var _ TenantContactRepository = (*TenantContactRepositoryImpl)(nil)

func (r *TenantContactRepositoryImpl) Save(ctx context.Context, e *entity.TenantContact) error {
	if e.Id == "" {
		// INSERT
		err := r.db.QueryRow(ctx, `
			INSERT INTO tenant_contacts (organization_id, tenant_id, contact_type, contact_value, is_primary)
			VALUES ($1, $2, $3, $4, $5)
			RETURNING id`,
			e.OrganizationId, e.TenantId, e.ContactType, e.ContactValue, e.IsPrimary,
		).Scan(&e.Id)
		return err
	}
	// UPDATE
	// Note: id is dynamically added as the last argument in Exec for UPDATE
	_, err := r.db.Exec(ctx, `
		UPDATE tenant_contacts
		SET    organization_id=$1, tenant_id=$2, contact_type=$3, contact_value=$4, is_primary=$5
		WHERE  id=$6 AND deleted_at IS NULL`,
		e.OrganizationId, e.TenantId, e.ContactType, e.ContactValue, e.IsPrimary, e.Id,
	)
	return err
}

func (r *TenantContactRepositoryImpl) FindByID(ctx context.Context, id string) (*entity.TenantContact, error) {
	e := &entity.TenantContact{}
	err := r.db.QueryRow(ctx, `
		SELECT organization_id, id, tenant_id, contact_type, contact_value, is_primary, deleted_at
		FROM   tenant_contacts
		WHERE  id = $1 AND deleted_at IS NULL`,
		id,
	).Scan(&e.OrganizationId, &e.Id, &e.TenantId, &e.ContactType, &e.ContactValue, &e.IsPrimary, &e.DeletedAt)
	
	if err != nil {
		return nil, fmt.Errorf("tenantcontact repository: find by id: %w", err)
	}
	return e, nil
}

func (r *TenantContactRepositoryImpl) FindAll(ctx context.Context, limit, offset int) ([]*entity.TenantContact, error) {
	rows, err := r.db.Query(ctx, `
		SELECT organization_id, id, tenant_id, contact_type, contact_value, is_primary, deleted_at
		FROM   tenant_contacts
		WHERE  deleted_at IS NULL
		ORDER BY created_at DESC
		LIMIT $1 OFFSET $2`,
		limit, offset,
	)
	if err != nil {
		return nil, fmt.Errorf("tenantcontact repository: find all: %w", err)
	}
	defer rows.Close()

	var list []*entity.TenantContact
	for rows.Next() {
		e := &entity.TenantContact{}
		if err := rows.Scan(&e.OrganizationId, &e.Id, &e.TenantId, &e.ContactType, &e.ContactValue, &e.IsPrimary, &e.DeletedAt); err != nil {
			return nil, err
		}
		list = append(list, e)
	}
	return list, rows.Err()
}

func (r *TenantContactRepositoryImpl) Delete(ctx context.Context, id string) error {
	_, err := r.db.Exec(ctx, `
		UPDATE tenant_contacts SET deleted_at = now() WHERE id = $1 AND deleted_at IS NULL`, id)
	return err
}
